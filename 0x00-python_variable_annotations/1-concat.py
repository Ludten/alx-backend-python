#!/usr/bin/env python3
"""
concatenate to two strings
"""


def concat(str1: str, str2: str) -> str:
    """
    concatenate to two strings

    Args:
        str1 (str)
        str2 (str)
    Returns:
        string
    """

    return '{}{}'.format(str1, str2)
